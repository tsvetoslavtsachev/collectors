"""VRM weekly collector — third citizen of data-core (INIT-22 E4/E5).

Un-freezes the VRM canonical series (prices + macro + market-context) that the
regime / overlay engines read. yfinance + FRED auto-feeds, ISM manual slot.
"""
